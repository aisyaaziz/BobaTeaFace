@model Course
                <!-- Page Content -->

                <div class="mdk-box bg-primary mdk-box--bg-gradient-primary2 js-mdk-box mb-0"
                     data-effects="blend-background">
                    <div class="mdk-box__content">
                        <div class="hero py-64pt text-center text-sm-left">
                            <div class="container page__container">
                                <h1 class="text-white">@Model.Title</h1>
                                <p class="lead text-white-50 measure-hero-lead mb-24pt">@Model.Description</p>
                            </div>
                        </div>

                    </div>
                </div>

                <div class="container page__container">
                    <div class="row">
                        <div class="col-lg-12">
                            <div class="border-left-2 page-section pl-32pt">
                                <div id="div1">

                    <div class="d-flex align-items-center page-num-container">
                        <div class="page-num">1</div>
                        <h4>@Model.WhoQ</h4>
                    </div>

                    <div class="card">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-4">
                                    @if (string.IsNullOrEmpty(Model.ExternalImageUrl))
                                    {
                                        <img src="@Model.ChildImageUrl" style="width:100%;" />
                                    }
                                    else
                                    {
                                        <img src="@Model.ExternalImageUrl" style="width:100%;" />
                                    }
                                </div>
                                <div class="col-md-8">

                                    <div class="form-group">
                                        <label class="form-label">Answer:</label>

                                        <textarea name="WhoA" class="form-control" placeholder="@Model.WhoQ"></textarea>
                                        <br />
                                        <button type="button" class="btn btn-secondary" onclick="$('#divWhoA').show(1000);">See Answer</button>
                                        <div id="divWhoA" style="display:none;">
                                            <br />
                                            <label class="form-label">Answer: @Model.WhoA</label>
                                            <br /><br />
                                            <button type="button" class="btn btn-primary" onclick="$('#div2').show(1000);$('#div1').hide(500);">Next Question</button>
                                        </div>


                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                                </div>

                <div id="div2" style="display:none;">
                    <div class="d-flex align-items-center page-num-container">
                        <div class="page-num">2</div>
                        <h4>@Model.WhatQ</h4>
                    </div>

                    <div class="card">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-4">
                                    @if (string.IsNullOrEmpty(Model.ExternalImageUrl))
                                    {
                                        <img src="@Model.ChildImageUrl" style="width:100%;" />
                                    }
                                    else
                                    {
                                        <img src="@Model.ExternalImageUrl" style="width:100%;" />
                                    }
                                </div>
                                <div class="col-md-8">

                                    <div class="form-group">
                                        <label class="form-label">Answer:</label>

                                        <textarea name="WhatA" class="form-control" placeholder="@Model.WhatQ"></textarea>
                                        <br />
                                        <button type="button" class="btn btn-secondary" onclick="$('#divWhatA').show(1000);">See Answer</button>
                                        <div id="divWhatA" style="display:none;">
                                            <br />
                                            <label class="form-label">Answer: @Model.WhatA</label>
                                            <br /><br />
                                            <button type="button" class="btn btn-primary" onclick="$('#div3').show(1000);$('#div2').hide(500);">Next Question</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

</div>


<div id="div3" style="display:none;">

                    <div class="d-flex align-items-center page-num-container">
                        <div class="page-num">3</div>
                        <h4>@Model.WhereQ</h4>
                    </div>

                    <div class="card">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-4">
                                    @if (string.IsNullOrEmpty(Model.ExternalImageUrl))
                                    {
                                        <img src="@Model.ChildImageUrl" style="width:100%;" />
                                    }
                                    else
                                    {
                                        <img src="@Model.ExternalImageUrl" style="width:100%;" />
                                    }
                                </div>
                                <div class="col-md-8">

                                    <div class="form-group">
                                        <label class="form-label">Answer:</label>

                                        <textarea name="WhereA" class="form-control" placeholder="@Model.WhereQ"></textarea>
                                        <br />
                                        <button type="button" class="btn btn-secondary" onclick="$('#divWhereA').show(1000);">See Answer</button>
                                        <div id="divWhereA" style="display:none;">
                                            <br />
                                            <label class="form-label">Answer: @Model.WhereA</label>
                                            <br /><br />
                                            <button type="button" class="btn btn-primary" onclick="$('#div4').show(1000);$('#div3').hide(500);">Next Question</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

</div>

<div id="div4" style="display:none;">

                    <div class="d-flex align-items-center page-num-container">
                        <div class="page-num">4</div>
                        <h4>@Model.WhenQ</h4>
                    </div>

                    <div class="card">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-4">
                                    @if (string.IsNullOrEmpty(Model.ExternalImageUrl))
                                    {
                                        <img src="@Model.ChildImageUrl" style="width:100%;" />
                                    }
                                    else
                                    {
                                        <img src="@Model.ExternalImageUrl" style="width:100%;" />
                                    }
                                </div>
                                <div class="col-md-8">

                                    <div class="form-group">
                                        <label class="form-label">Answer:</label>

                                        <textarea name="WhenA" class="form-control" placeholder="@Model.WhenQ"></textarea>
                                        <br />
                                        <button type="button" class="btn btn-secondary" onclick="$('#divWhenA').show(1000);">See Answer</button>
                                        <div id="divWhenA" style="display:none;">
                                            <br />
                                            <label class="form-label">Answer: @Model.WhenA</label>
                                            <br /><br />
                                            <button type="button" class="btn btn-primary" onclick="$('#div5').show(1000);$('#div4').hide(500);">Next Question</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>

                <div id="div5" style="display:none;">
                    <div class="d-flex align-items-center page-num-container">
                        <div class="page-num">5</div>
                        <h4>@Model.WhyQ</h4>
                    </div>

                    <div class="card">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-4">
                                    @if (string.IsNullOrEmpty(Model.ExternalImageUrl))
                                    {
                                        <img src="@Model.ChildImageUrl" style="width:100%;" />
                                    }
                                    else
                                    {
                                        <img src="@Model.ExternalImageUrl" style="width:100%;" />
                                    }
                                </div>
                                <div class="col-md-8">

                                    <div class="form-group">
                                        <label class="form-label">Answer:</label>

                                        <textarea name="WhyA" class="form-control" placeholder="@Model.WhyQ"></textarea>
                                        <br />
                                        <button type="button" class="btn btn-secondary" onclick="$('#divWhyA').show(1000);">See Answer</button>
                                        <div id="divWhyA" style="display:none;">
                                            <br />
                                            <label class="form-label">Answer: @Model.WhyA</label>
                                            <br /><br />
                                            <button type="button" class="btn btn-primary" onclick="$('#div6').show(1000);$('#div5').hide(500);">Next Question</button>
                                        </div>

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>

                <div id="div6" style="display:none;">
                    <div class="d-flex align-items-center page-num-container">
                        <div class="page-num">6</div>
                        <h4>@Model.HowQ</h4>
                    </div>

                    <div class="card">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-4">
                                    @if (string.IsNullOrEmpty(Model.ExternalImageUrl))
                                    {
                                        <img src="@Model.ChildImageUrl" style="width:100%;" />
                                    }
                                    else
                                    {
                                        <img src="@Model.ExternalImageUrl" style="width:100%;" />
                                    }
                                </div>
                                <div class="col-md-8">

                                    <div class="form-group">
                                        <label class="form-label">Answer:</label>

                                        <textarea name="HowA" class="form-control" placeholder="@Model.HowQ"></textarea>
                                        <br />
                                        <button type="button" class="btn btn-secondary" onclick="finalAnswer();">See Answer</button>
                                        <div id="divHowA" style="display:none;">
                                            <br />
                                            <label class="form-label">Answer: @Model.HowA</label>
                                            <br />

                                            <a href="@Url.Action("")" class="btn btn-outline-primary" >Back to Courses</a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

</div>





                            </div>
                        </div>
                      
                    </div>
                </div>

                <!-- // END Page Content -->
@section Scripts{
    <script src="@Url.Content("~/js/jquery.confetti.js")"></script>
    <script type="text/javascript">
    function finalAnswer(){
            $('#divHowA').show(1000);
            $.confetti.start(); 
            setTimeout(
                function () {
                    $.confetti.stop();
                    //do something special
                }, 4000);
    }    
    </script>
                }